# GitHub Pages 零代码部署

当前项目文件已经在这个文件夹里：`growth-memo`。

## 方式一：让 Codex 继续上传

1. 打开 GitHub。
2. 新建一个空仓库，建议名字叫 `growth-memo`。
3. 不要勾选添加 README。
4. 把仓库链接发给 Codex。

## 方式二：自己用网页上传

1. 打开 GitHub，新建仓库 `growth-memo`。
2. 进入仓库，点 `uploading an existing file`。
3. 把 `growth-memo` 文件夹里的所有内容拖进去。
4. 点绿色 `Commit changes`。
5. 进入 `Settings` -> `Pages`。
6. `Source` 选择 `Deploy from a branch`。
7. `Branch` 选择 `main`，目录选择 `/root`。
8. 保存后等待一两分钟，页面会出现你的网址。

## iPhone 添加到主屏幕

1. 用 Safari 打开 GitHub Pages 网址。
2. 点分享按钮。
3. 点 `添加到主屏幕`。
4. 名字填 `生长备忘录`。
